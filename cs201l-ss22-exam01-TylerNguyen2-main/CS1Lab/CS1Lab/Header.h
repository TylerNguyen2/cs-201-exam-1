#ifndef Header_h

#define Header_h

#include <vector>
using namespace std;


double calculateTotal(vector<double>);
// Calculate the total amount of the catergory
double calculateMedian(vector<double>);

// calculate the middle number

double calculateMean(vector<double>);
// calculate the mean

#endif